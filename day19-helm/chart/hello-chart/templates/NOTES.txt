hello-chart has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Check status:
  kubectl get pods,svc -l app.kubernetes.io/instance={{ .Release.Name }}

Reach it:
{{- if .Values.ingress.enabled }}
  curl http://{{ .Values.ingress.host }}/    (after adding it to /etc/hosts)
{{- else }}
  kubectl port-forward svc/{{ include "hello-chart.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/
{{- end }}
