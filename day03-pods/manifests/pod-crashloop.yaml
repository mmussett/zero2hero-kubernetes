# Intentionally exits non-zero to demonstrate CrashLoopBackOff.
apiVersion: v1
kind: Pod
metadata:
  name: crash-demo
spec:
  restartPolicy: Always
  containers:
    - name: crasher
      image: busybox:1.36
      command: ["sh", "-c", "echo 'about to crash'; sleep 2; exit 1"]
